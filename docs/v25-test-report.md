# v25.1 角色、首页、场景与音效回归报告

测试日期：2026-07-17

## 自动检查

- 全部 `js/*.js` 通过 `node --check`。
- `git diff --check` 通过。
- 六主题扩展均为 50 个角色，总计 300；每主题名称和 ID 唯一。
- 六主题素材目录均为 50 张，合计 300 张 512×512 WebP，无空文件。
- 卡牌适配器验证：怪物卡输出角色 `<img>`，知识卡仍输出原 emoji。
- CSS 花括号检查通过：`style.css` 和 `scenes.css` 均配对。

## 本地浏览器检查

- Computer Use 实测用户首屏正常加载：三位主角、两只主题妖怪、用户卡均清晰可见。
- 首页英雄横幅、当前用户角色、300 妖怪提示和六张主题角色卡均正常加载。
- 六张主题入口均保留原 data-module，点击“小九九速算”可进入原模式选择流程。
- 小九九模式页新增算术王国远征队横幅，原章节、难度、经典练习和图鉴入口仍在。
- 小九九战斗入口正常，图鉴计数从原内容基础上增加 50（本地显示 0/154）。
- 战斗内放大后的主角、攻击武器层和妖怪图片均加载成功，无缺失图片；静止时不重复悬挂武器，出手时仍显示武器层与弹道。
- 战斗舞台同时包含 `module-theme-xiaojiujiu` 和属性 mood 类。
- 主题星层、地面纹样和 v25.1 远景地标的计算样式已生效。
- Chrome 响应式设备模式 400×905 实测：人物、怪物、题目和四个答案均在视口内，无横向溢出。

## 保持不变

题库、答案、难度、计时、伤害、连击阈值、掉落率、胜负条件、章节解锁和本地存储键未修改。
