# v25.0 角色、场景与音效回归报告

测试日期：2026-07-17

## 自动检查

- 全部 `js/*.js` 通过 `node --check`。
- `git diff --check` 通过。
- 六主题扩展均为 50 个角色，总计 300；每主题名称和 ID 唯一。
- 六主题素材目录均为 50 张，合计 300 张 512×512 WebP，无空文件。
- 卡牌适配器验证：怪物卡输出角色 `<img>`，知识卡仍输出原 emoji。
- CSS 花括号检查通过：`style.css` 和 `scenes.css` 均配对。

## 本地浏览器检查

- 首页正常加载，整体星尘与径向光背景可见。
- 小九九战斗入口正常，图鉴计数从原内容基础上增加 50（本地显示 0/154）。
- 战斗内主角、武器和妖怪图片均加载成功，无缺失图片。
- 战斗舞台同时包含 `module-theme-xiaojiujiu` 和属性 mood 类。
- 主题星层、地面纹样的计算样式已生效。
- 页面控制台没有 error / warn。

## 保持不变

题库、答案、难度、计时、伤害、连击阈值、掉落率、胜负条件、章节解锁和本地存储键未修改。
