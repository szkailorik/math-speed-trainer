# v26.0 玩家成长、地图与战斗回归报告

测试日期：2026-07-18

## 自动检查

- 全部 `js/*.js` 通过 `node --check`，`git diff --check` 通过。
- 冒险状态行为测试通过：旧分数迁移、旧塔资格转铜钥匙、购买扣分、积分不足拦截、装备切换、塔门消耗和地图解锁。
- 六主题扩展仍各 50 个角色，总计 300；各主题名称和 ID 唯一。
- 六主题素材目录仍各 50 张非空 WebP，总计 300。
- 本轮未修改题库答案、难度、基础伤害、计时、掉落率或章节星级门槛。

## Computer Use 浏览器检查

- 在 `http://localhost:5178` 首次选择玩家后显示三格开场故事，并可进入角色创建。
- 将角色改为“测试勇士”、女性、冒险卷发并保存；刷新后首页姓名、女性立绘和发型层仍保留。
- 商店显示 0 可用积分和每件商品价格；点击 240 分商品会提示“积分不足，还差 240 分”，没有加入库存。
- 冒险地图只允许进入晨光村；森林、洞穴、荒漠、城堡、雪峰和塔均显示尚未解锁且按钮禁用。
- 从首页直接点试炼之塔会说明需要铜钥匙；确认后自动打开地图，没有出现旧 10 分钟倒计时。
- 晨光村任务按钮进入原小九九模式选择，继续点击“怪兽战斗”可正常开始真实关卡。
- 战斗显示已保存角色与长剑层；答对 `8 × 6` 后可访问树捕捉到长剑命中符号 `╳`、`-1` 伤害、受伤台词，怪物 HP 从 3/3 变为 2/3。
- 页面截图确认人物、怪物、HP、题目和四个答案均清晰；本地服务器访问日志无 404。
- 在线 Computer Use 发现并修复跨页面保留旧滚动位置的问题；所有 `showPage()` 切换现在统一回到页面顶部，短剧情页不会误显示成空白。

## 缓存与存档

- 本轮修改过的 CSS / JS URL 均升级为 `v=26.0.0`，避免 Cloudflare 或浏览器继续使用 v25 缓存。
- 仍沿用 `mathSpeedTrainer_users`、`mathSpeedTrainer_currentUser` 和 `mathSpeedTrainer_<userId>`；新字段缺失时自动补齐。
